<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About - Lego Collection</title>
    <!-- <link href="../public/css/main.css" rel="stylesheet"> --> <!--local testing i.e run this with direct link-->
    <link href="/css/main.css" rel="stylesheet"> <!-- live server -->
</head>
<body>
    <%- include('partials/navbar', {page : ''}) %>
    <!-- Add a header with tailwind css -->
    <div class="container mx-auto mt-8">
        <div class ="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold"><%= data.name %></h1>
                    <p class="py-6">Explore the amazing world of Lego sets, themes, and more!</p>
                </div>
            </div>
        </div>
        <br>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                 <img class="w-full rounded-lg" src="<%= data.img_url %>" alt="<%= data.name %>">
              </div>  
              <div class="prose">
                <span class="text-2xl font-bold">Set Name:</span> <%= data.name %><br>
                <span class="text-2xl font-bold">Year Released:</span> <%= data.year %><br>
                <span class="text-2xl font-bold">Theme:</span> <%= data.theme_name %><br>
                <span class="text-2xl font-bold">Number of Parts:</span> <%= data.num_parts %><br><br>
                <span id="quote">" <%= data.quote %> "</span> <br>
                <em id="author"> - <%= data.character %></em>
                <br><br>
                <a class="btn btn-primary btn-sm" href="#" onclick="history.back(); return false;">Return</a>
              </div>
        </div>
    </div>   
    <script>
            document.addEventListener("DOMContentLoaded", ()=>{
                fetch("https://quotable.io/random")
                .then(response => response.json())
                .then(data => {
                    document.getElementById("quote").innerHTML = data.content;
                    document.getElementById("author").innerHTML = data.author;
                })
                .catch(error => console.error(error));


            }); 
    </script>
   
</body>
</html>
